<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Dancercity" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="off-canvas-wrap" data-offcanvas>
      <div class="inner-wrap">
        <nav class="tab-bar">
          <section class="left-small">
            <a class="left-off-canvas-toggle menu-icon" ><span></span></a>
          </section>

          <section class="middle tab-bar-section">
            <h1 class="title"><%= link_to 'Dancer City', root_path %></h1>
          </section>
        </nav>

        <aside class="left-off-canvas-menu">
          <ul class="off-canvas-list">
            <li><label>Menu</label></li>
            <li><a href="#">Dasdboard</a></li>
            <% if current_user %>
              <li><%= link_to 'Profile', user_path(current_user.id) %></li>
            <% end %>
          </ul>
        </aside>


        <section class="main-section">
          <div class="row">
            <div class="small-12 small-centered columns">
              <% flash.each do |name, message| %>
                <%= content_tag :div, class: 'alert-box success radius', data: {alert: ''} do %>
                  <%= message %>
                  <a href="#" class="close">&times;</a>
                <% end %>
              <% end %>

              <% if current_user %>
                Signed in as <strong><%= current_user.first_name %></strong>!
                <%= link_to "Sign out", signout_path, id: "sign_out", class: 'button radius' %>
              <% else %>
                <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in", class: 'button radius' %>
              <% end %>
              
              <%= yield %>
            </div>
          </div>
        </section>

        <a class="exit-off-canvas"></a>

      </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
